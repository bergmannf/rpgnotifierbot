{
    "name": "rpg notifier bot devcontainer",
    "remoteEnv": {
        "PATH": "${containerEnv:PATH}:/usr/local/go/bin:/go/bin"
    },
    "remoteUser": "root",
    "build": {
        "dockerfile": "Dockerfile"
    }
}
